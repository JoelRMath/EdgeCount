% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ECTermScoring.R
\name{calculate_ecset_fdr}
\alias{calculate_ecset_fdr}
\title{Calculate Empirical FDR for Set Enrichment}
\usage{
calculate_ecset_fdr(
  object,
  real_results_dt,
  n_permutations = 1000,
  seed = NULL
)
}
\arguments{
\item{object}{An \code{\link{ECTermScoring}} object.}

\item{real_results_dt}{A \code{data.table} or \code{data.frame} containing the
scoring results for a single set (e.g., the output of \code{\link{terms_ecset_statistics}}).
Must contain columns `observed_edge_count` and `log2_Anscombe_ratio`.}

\item{n_permutations}{Integer. The number of random sets to generate for the
null distribution. Defaults to 1000.}

\item{seed}{Integer (Optional). A random seed for reproducibility.}
}
\value{
A \code{data.table} containing the original results with two additional columns:
\itemize{
  \item \code{nes}: Normalized Enrichment Score.
  \item \code{fdr_q_value}: The empirical False Discovery Rate q-value.
}
The table is sorted by \code{fdr_q_value} (ascending) and \code{nes} (descending).
}
\description{
Calculates Normalized Enrichment Scores (NES) and False Discovery
Rates (FDR) for term enrichment by comparing observed scores against a null
distribution generated from random sets.
}
\details{
This method addresses the non-uniform distribution of p-values in
sparse graphs under the RGGED null model.
}
\seealso{
\code{\link{terms_ecset_statistics}}, \code{\link{run_vsea}}
}
