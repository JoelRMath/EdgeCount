\documentclass{article}\usepackage[]{graphicx}\usepackage[]{xcolor}
% maxwidth is the original width if it is less than linewidth
% otherwise use linewidth (to make sure the graphics do not exceed the margin)
\makeatletter
\def\maxwidth{ %
  \ifdim\Gin@nat@width>\linewidth
    \linewidth
  \else
    \Gin@nat@width
  \fi
}
\makeatother

\definecolor{fgcolor}{rgb}{0.345, 0.345, 0.345}
\newcommand{\hlnum}[1]{\textcolor[rgb]{0.686,0.059,0.569}{#1}}%
\newcommand{\hlsng}[1]{\textcolor[rgb]{0.192,0.494,0.8}{#1}}%
\newcommand{\hlcom}[1]{\textcolor[rgb]{0.678,0.584,0.686}{\textit{#1}}}%
\newcommand{\hlopt}[1]{\textcolor[rgb]{0,0,0}{#1}}%
\newcommand{\hldef}[1]{\textcolor[rgb]{0.345,0.345,0.345}{#1}}%
\newcommand{\hlkwa}[1]{\textcolor[rgb]{0.161,0.373,0.58}{\textbf{#1}}}%
\newcommand{\hlkwb}[1]{\textcolor[rgb]{0.69,0.353,0.396}{#1}}%
\newcommand{\hlkwc}[1]{\textcolor[rgb]{0.333,0.667,0.333}{#1}}%
\newcommand{\hlkwd}[1]{\textcolor[rgb]{0.737,0.353,0.396}{\textbf{#1}}}%
\let\hlipl\hlkwb

\usepackage{framed}
\makeatletter
\newenvironment{kframe}{%
 \def\at@end@of@kframe{}%
 \ifinner\ifhmode%
  \def\at@end@of@kframe{\end{minipage}}%
  \begin{minipage}{\columnwidth}%
 \fi\fi%
 \def\FrameCommand##1{\hskip\@totalleftmargin \hskip-\fboxsep
 \colorbox{shadecolor}{##1}\hskip-\fboxsep
     % There is no \\@totalrightmargin, so:
     \hskip-\linewidth \hskip-\@totalleftmargin \hskip\columnwidth}%
 \MakeFramed {\advance\hsize-\width
   \@totalleftmargin\z@ \linewidth\hsize
   \@setminipage}}%
 {\par\unskip\endMakeFramed%
 \at@end@of@kframe}
\makeatother

\definecolor{shadecolor}{rgb}{.97, .97, .97}
\definecolor{messagecolor}{rgb}{0, 0, 0}
\definecolor{warningcolor}{rgb}{1, 0, 1}
\definecolor{errorcolor}{rgb}{1, 0, 0}
\newenvironment{knitrout}{}{} % an empty environment to be redefined in TeX

\usepackage{alltt}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{url}
\usepackage{geometry}
\geometry{verbose,tmargin=2.5cm,bmargin=2.5cm,lmargin=2.5cm,rmargin=2.5cm}
\usepackage{parskip}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

\sloppy

\title{EdgeCount: Statistical Theory and Algorithmic Efficiency}
\author{Joel R. Pradines}
\date{\today}
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\begin{document}



\maketitle

\begin{abstract}
\noindent This vignette details the statistical framework underlying the \texttt{EdgeCount} package. First, the Random Graph with Given Expected Degrees (RGGED) model is described. Second, the derivation of edge-count statistics is presented. Third, the algebraic optimizations that allow the package to calculate connectedness statistics with linear time complexity are explained. Last, the implementation of vectorized functions contained in the package is discussed, and it is shown that they have optimal time complexity.
\end{abstract}

\tableofcontents

\section{Introduction}

The \texttt{EdgeCount} package provides methods to analyze the connectedness of vertex sets in large, sparse, undirected graphs. Whether analyzing interaction networks or bipartite group-element graphs, the question is:

\textit{Is an observed number of edges, called edge count, within a set (or between two sets) significantly higher than expected by chance?}

To answer this, two components must be implemented.

First, the random experiment defining the concept of \textit{chance} must be clearly specified and, in general, simulated to provide a control distribution.

Second, one needs to be able to fairly compare edge-count values to each other. A fair comparison requires accounting for vertex degrees, as these may vary by orders of magnitude. This is achieved here by using a null model of random graphs that preserves the expected degrees of vertices \cite{chung2002connected,RGGED2}. Specifically, the average degree of a vertex over all random graph realizations matches its observed value in the original network. The analytical expressions for the likelihoods of edge-count variables in this model were derived in \cite{JCB}. These expressions enable fair and fast comparisons of edge-count values.


\section{Random graph model}

Let $G(V, E)$ be an undirected graph with no loops and no multiple edges.
\begin{itemize}
    \item Let $N = |V|$ be the order of the graph (number of vertices).
    \item Let $M = |E|$ be the size of the graph (number of edges).
    \item Let $k_i$ be the degree of vertex $v_i$.
    \item Let $\left< k \right>$ be the the average vertex degree.
\end{itemize}

Call degree sequence of $G$ the nonincreasing sequence of all vertex degrees. An obvious null model for $G$ is that of a random graph whose realizations always preserve the degree sequence. This model is referred to as the Random Graph with Fixed Degree Sequence (RGFDS). However, it is known that the analytical treatment of the RGFDS is a difficult problem \cite{bender1978asymptotic, molloy1995critical, itzkovitz2003subgraphs, park2004statistical}. Namely, deriving simple analytical expressions for the likelihoods of edge-count variables is in general hard with the RGFDS. This implies that slow numerical methods based on simulations must be used \cite{maslov2002specificity, milo2002network, milo2003uniform}.

A less constrained random graph model than the RGFDS is the Random Graph with Given Expected Degrees (RGGED) \cite{chung2002connected,RGGED2}.  While an individual realization of the RGGED might not preserve a vertex degree, it is preserved on average over all realizations. The key feature of the RGGED is that edges are treated as independent random variables, enabling the derivation of analytical expressions for the distributions of edge-count variables \cite{JCB}.

In the RGGED model, edges are treated as independent Bernoulli random variables: an edge $v_iv_j$ exists with probability $p_{ij}$ (Bernoulli parameter) and does not exist with probability $1-p_{ij}$. When the graph is large and sparse on average over its vertices ($\left< k \right>^2 \ll 2M$), the probability $p_{ij}$ of observing an edge between vertices $v_i$ and $v_j$ is given by \cite{JCB}:
\begin{equation}
p_{ij} \simeq 1 - \exp\left(-k_i k_j/(2M)\right).
\end{equation}
Useful upper bounds for $p_{ij}$ are
\begin{equation}
p_{ij} \leq \min\left( 1, \frac{k_ik_j}{2M}\right) \leq \frac{k_ik_j}{2M}.
\end{equation}

\subsection{Within and between variables}

The \texttt{EdgeCount} package considers two types of edge-count variables:
\begin{itemize}
\item "within" for the number $z$ of edges observed in a given set $S$ of vertices
\item "between" for the number $z$ of edges observed between two sets $S_1$ and $S_2$ of vertices
\end{itemize}
Observed values $z$ are obviously dependent on set sizes, but they should also be made conditional to degrees of the vertices in the set(s). Hence, to an observed value $z$ is associated a corresponding random variable $Z$ in the RGGED.

\subsection{Edge-count distribution}

Edge-count random variables in the RGGED model of a large and sparse graph have approximate Poisson distribution. This is because they are the sum of independent Bernoulli random variables with small parameters $p_{ij} \ll 1$, even when these parameters have different values \cite{lecam1960approximation, kerstan1964verallgemeinerung, JCB}.

Call $Z$ an edge-count random variable defined by the sum of $m$ potential edges, each edge corresponding to a pair $v_iv_j$ of vertices:
\begin{equation}
Z = \sum_{u=1}^m B_u \quad \text{with} \quad B_u \sim \text{Bernoulli} \left(p_u = p_{ij} \right).
\end{equation}

Then, we have $Z \sim \text{Poisson} \left(\lambda, m\right)$ as defined by

\begin{equation}
\Pr \left( Z = z \right) = \frac{e^{-\lambda}}{\alpha}\frac{\lambda^z}{z!} \quad \text{with} \quad \lambda = \sum_{u=1}^m p_u \quad \text{and} \quad
\alpha = e^{-\lambda} \sum_{x=0}^m \frac{\lambda^x}{x!}.
\end{equation}

The normalization factor $\alpha$ is utilized because an observed number $z$ of edges is bounded by a maximum number $m$ of edges. This said, we have $\alpha \simeq 1$ when $p_{ij} \ll 1$. This is numerically illustrated next.


\begin{knitrout}
\definecolor{shadecolor}{rgb}{0.969, 0.969, 0.969}\color{fgcolor}\begin{kframe}
\begin{alltt}
\hlcom{# Function to calculate alpha and its complement}
\hldef{get_alpha_stats} \hlkwb{<-} \hlkwa{function}\hldef{(}\hlkwc{m}\hldef{,} \hlkwc{pij_avg}\hldef{) \{}
  \hldef{lambda} \hlkwb{<-} \hldef{m} \hlopt{*} \hldef{pij_avg}
  \hldef{alpha} \hlkwb{<-} \hldef{stats}\hlopt{::}\hlkwd{ppois}\hldef{(m, lambda)}
  \hldef{alpha_comp} \hlkwb{<-} \hldef{stats}\hlopt{::}\hlkwd{ppois}\hldef{(m, lambda,} \hlkwc{lower.tail} \hldef{=} \hlnum{FALSE}\hldef{)}
  \hlkwd{return}\hldef{(}\hlkwd{c}\hldef{(alpha, alpha_comp))}
\hldef{\}}
\hlcom{# Parameters}
\hldef{pij_values} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{0.001}\hldef{,} \hlnum{0.01}\hldef{,} \hlnum{0.05}\hldef{,} \hlnum{0.1}\hldef{)}
\hldef{m_values} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{1}\hlopt{:}\hlnum{29}\hldef{,}\hlkwd{seq}\hldef{(}\hlnum{30}\hldef{,} \hlnum{1000}\hldef{,} \hlkwc{by} \hldef{=} \hlnum{10}\hldef{))}
\hldef{colors} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlsng{"red"}\hldef{,} \hlsng{"blue"}\hldef{,} \hlsng{"green"}\hldef{,} \hlsng{"black"}\hldef{)}
\hlcom{# Plots}
\hlkwd{par}\hldef{(}\hlkwc{mfrow}\hldef{=}\hlkwd{c}\hldef{(}\hlnum{1}\hldef{,} \hlnum{2}\hldef{),} \hlkwc{mar}\hldef{=}\hlkwd{c}\hldef{(}\hlnum{5}\hldef{,} \hlnum{5}\hldef{,} \hlnum{4}\hldef{,} \hlnum{2}\hldef{)} \hlopt{+} \hlnum{0.1}\hldef{,} \hlkwc{cex.lab}\hldef{=}\hlnum{1.25}\hldef{,}
    \hlkwc{cex.axis}\hldef{=}\hlnum{1.25}\hldef{,} \hlkwc{cex.main}\hldef{=}\hlnum{1.25}\hldef{)}
\hlkwd{plot}\hldef{(}\hlnum{1}\hldef{,} \hlkwc{type}\hldef{=}\hlsng{"n"}\hldef{,} \hlkwc{xlab}\hldef{=}\hlsng{"Maximum possible edges (m)"}\hldef{,}
     \hlkwc{ylab}\hldef{=}\hlkwd{expression}\hldef{(alpha),}
     \hlkwc{xlim}\hldef{=}\hlkwd{c}\hldef{(}\hlnum{1}\hldef{,} \hlnum{1000}\hldef{),} \hlkwc{ylim}\hldef{=}\hlkwd{c}\hldef{(}\hlnum{0.99}\hldef{,} \hlnum{1.00}\hldef{),}
     \hlkwc{log}\hldef{=}\hlsng{"x"}\hldef{,}
     \hlkwc{main}\hldef{=}\hlkwd{expression}\hldef{(}\hlkwd{paste}\hldef{(}\hlsng{"Value of "}\hldef{, alpha,} \hlsng{" (Linear)"}\hldef{)))}
\hlkwa{for}\hldef{(i} \hlkwa{in} \hlkwd{seq_along}\hldef{(pij_values)) \{}
  \hldef{lambdas} \hlkwb{<-} \hldef{m_values} \hlopt{*} \hldef{pij_values[i]}
  \hldef{y_vals} \hlkwb{<-} \hlkwd{ppois}\hldef{(m_values, lambdas)}
  \hlkwd{lines}\hldef{(m_values, y_vals,} \hlkwc{col}\hldef{=colors[i],} \hlkwc{lwd}\hldef{=}\hlnum{2}\hldef{)}
\hldef{\}}
\hlkwd{legend}\hldef{(}\hlsng{"bottomright"}\hldef{,} \hlkwc{legend}\hldef{=}\hlkwd{paste}\hldef{(}\hlsng{"pij ="}\hldef{, pij_values),}
       \hlkwc{col}\hldef{=colors,} \hlkwc{lwd}\hldef{=}\hlnum{2}\hldef{,} \hlkwc{bty}\hldef{=}\hlsng{"n"}\hldef{,} \hlkwc{cex}\hldef{=}\hlnum{1}\hldef{)}
\hldef{epsilon} \hlkwb{<-} \hlnum{1e-16}
\hlkwd{plot}\hldef{(}\hlnum{1}\hldef{,} \hlkwc{type}\hldef{=}\hlsng{"n"}\hldef{,} \hlkwc{xlab}\hldef{=}\hlsng{"Maximum possible edges (m)"}\hldef{,}
     \hlkwc{ylab}\hldef{=}\hlkwd{expression}\hldef{(}\hlnum{1} \hlopt{-} \hldef{alpha),}
     \hlkwc{xlim}\hldef{=}\hlkwd{c}\hldef{(}\hlnum{1}\hldef{,} \hlnum{1000}\hldef{),} \hlkwc{ylim}\hldef{=}\hlkwd{c}\hldef{(epsilon,} \hlnum{1}\hldef{),}
     \hlkwc{log}\hldef{=}\hlsng{"xy"}\hldef{,}
     \hlkwc{main}\hldef{=}\hlkwd{expression}\hldef{(}\hlkwd{paste}\hldef{(}\hlsng{"Error "}\hldef{,} \hlnum{1} \hlopt{-} \hldef{alpha,} \hlsng{" (Log Scale)"}\hldef{)))}
\hlkwa{for}\hldef{(i} \hlkwa{in} \hlkwd{seq_along}\hldef{(pij_values)) \{}
  \hldef{lambdas} \hlkwb{<-} \hldef{m_values} \hlopt{*} \hldef{pij_values[i]}
  \hldef{y_vals} \hlkwb{<-} \hlkwd{ppois}\hldef{(m_values, lambdas,} \hlkwc{lower.tail} \hldef{=} \hlnum{FALSE}\hldef{)}
  \hldef{y_vals} \hlkwb{<-} \hlkwd{pmax}\hldef{(y_vals, epsilon)}
  \hlkwd{lines}\hldef{(m_values, y_vals,} \hlkwc{col}\hldef{=colors[i],} \hlkwc{lwd}\hldef{=}\hlnum{2}\hldef{)}
\hldef{\}}
\hlkwd{legend}\hldef{(}\hlsng{"topright"}\hldef{,} \hlkwc{legend}\hldef{=}\hlkwd{paste}\hldef{(}\hlsng{"pij ="}\hldef{, pij_values),}
       \hlkwc{col}\hldef{=colors,} \hlkwc{lwd}\hldef{=}\hlnum{2}\hldef{,} \hlkwc{bty}\hldef{=}\hlsng{"n"}\hldef{,} \hlkwc{cex}\hldef{=}\hlnum{1}\hldef{)}
\end{alltt}
\end{kframe}

{\centering \includegraphics[width=1\textwidth]{figure/alpha_simulation_side_by_side-1} 

}


\end{knitrout}

For a within variable associated with a set $S$ we have
\begin{equation}
m = \left| S \right| \left(\left| S \right| -1 \right)/2 \quad \text{and} \quad \lambda = \sum_{i < j, \in S} p_{ij}.
\end{equation}
For a between variable associated with two sets $S_1$ and $S_2$ we have
\begin{equation}
m = \left| S_1 \right| \left| S_2 \right|  \quad \text{and} \quad \lambda = \sum_{i \in S_1, j \in S_2} p_{ij}.
\end{equation}

Since the distribution of an edge-count variable is easily computed, p-values are also calculated.

\begin{equation}
\Pr\left( Z \geq z \right) = \sum_{x=z}^{m} \Pr \left( Z = x\right).
\end{equation}

P-values are calculated with \texttt{R} function \texttt{ppois}.

\begin{knitrout}
\definecolor{shadecolor}{rgb}{0.969, 0.969, 0.969}\color{fgcolor}\begin{kframe}
\begin{alltt}
\hldef{alpha} \hlkwb{<-} \hldef{stats}\hlopt{::}\hlkwd{ppois}\hldef{(m, lambda,} \hlkwc{lower.tail} \hldef{=} \hlnum{TRUE}\hldef{)}
\hldef{p_value} \hlkwb{<-} \hldef{(stats}\hlopt{::}\hlkwd{ppois}\hldef{(z} \hlopt{-} \hlnum{1}\hldef{, lambda,} \hlkwc{lower.tail} \hldef{=} \hlnum{FALSE}\hldef{)} \hlopt{-}
              \hldef{stats}\hlopt{::}\hlkwd{ppois}\hldef{(m, lambda,} \hlkwc{lower.tail} \hldef{=} \hlnum{FALSE}\hldef{))} \hlopt{/} \hldef{alpha}
\end{alltt}
\end{kframe}
\end{knitrout}

The Poisson distribution enables quantification of the variability on vertex degree introduced by the RGGED. In the model, the degree of a vertex $v_i$ with initial degree $k_i$ is a random variable $K_i$ defined by the edge count between this vertex and all the others:
\begin{equation}
\lambda_i = \frac{k_i}{2M}\sum_{j\neq i}k_j \simeq k_i.
\end{equation}
That is, $K_i$ has mean $k_i$ and standard deviation $\sqrt{k_i}$. The introduced relative variability on $k_i$ is reasonable: $1/\sqrt{k_i}$.

\subsection{Edge-count effect size}

As explained earlier, the RGGED is not directly utilized as a null model to answer a question. Rather, it is used as an intermediate step that enables fair and fast comparison of diverse observed edge counts. Such a comparison can be between two edge-count variables defined by sets of different sizes. In this case, an effect size is the most appropriate quantity for comparison.

Because edge-count random variables have Poisson distribution, the utilized effect size is

\begin{equation}
\log_2 \text{Anscombe ratio:} \quad lAr \left(z, \lambda \right) = \frac{1}{2} \log_2 \frac{z + 3/8}{\lambda + 3/8}.
\end{equation}

The addition of 3/8 stems from the work of Anscombe \cite{anscombe1948}, who showed that $\sqrt{X + 3/8}$ is the optimal variance-stabilizing transformation for a Poisson random variable $X$.


\section{Fast approximations for sparse graphs}

A condition for sparseness in the \texttt{EdgeCount} framework is
\begin{equation}
\max_{i,j} \frac{k_ik_j}{2M} < 1.
\end{equation}

This sparseness condition can be tested with method \texttt{summarize\_suitability\_fast}. For unipartite graphs (interaction networks), the relevant method is that of class \texttt{ECProb}. For bipartite graphs (group-element networks), the appropriate method is that of class \texttt{ECTermScoring}.
The method returns a list containing the proportion \texttt{pij\_over\_1} of $(k_ik_j)/(2M) \geq 1$. An ideally sparse graph yields \texttt{pij\_over\_1} = 0.

Now, approximating $p_{ij}$ values with $k_ik_j/(2M)$ is a conservative approach in the RGGED. Namely, using this approximation
\begin{itemize}
\item[-] overestimates $p_{ij}$ values,
\item[-] overestimates lambda values,
\item[-] overestimates edge-count p-values,
\item[-] underestimates effect sizes.
\end{itemize}

Therefore, utilizing this approximation by default is a reasonable approach. The approximation is called {\bf fast approximation} because it enables faster calculation of edge-count statistics. The speedup is obtained in the computation of lambda parameters.

\subsection{Within set connectedness}

Consider a vertex set $S$ of size $\left| S \right| = n$. With the fast approximation the $\lambda$ parameter is
\begin{equation}
\lambda_{in} = \sum_{i < j, \in S} \frac{k_i k_j}{2M}.
\end{equation}
A naive implementation would have time complexity $\mathcal{O} \left( n^2 \right)$. Recall the expression for the square of a sum
\begin{equation}
\left( \sum_{i \in S} k_i \right)^2 = \sum_{i \in S} k_i^2 + 2 \sum_{i < j} k_i k_j.
\end{equation}
Rearranging the cross terms
\begin{equation}
\sum_{i < j} k_i k_j = \frac{1}{2} \left[ \left( \sum_{i \in S} k_i \right)^2 - \sum_{i \in S} k_i^2 \right],
\end{equation}
and substituting in the expression for lambda yields
\begin{equation}
\lambda_{in} = \frac{1}{4M} \left[ \left( \sum_{i \in S} k_i \right)^2 - \sum_{i \in S} k_i^2 \right].
\end{equation}

With this expression the computation of $\lambda_{in}$ has now linear time complexity: $\mathcal{O}\left( n \right)$. Method \texttt{edge\_count\_statistics()} of class \texttt{ECProb} when used with argument \texttt{lambda\_method = "fast"} implements this linear computation. Method \texttt{calculate\_in\_stats\_fast\_vectorized()} is an implementation of "within" edge count statistics for multiple sets provided as input. This method is fully vectorized and uses the \texttt{data.table} package \cite{datatable2025}. Here is a numerical illustration that its implementation achieves optimal, linear here, time complexity.

\begin{knitrout}
\definecolor{shadecolor}{rgb}{0.969, 0.969, 0.969}\color{fgcolor}\begin{kframe}
\begin{alltt}
\hlkwd{library}\hldef{(data.table)}
\hlkwd{library}\hldef{(EdgeCount)}
\hlkwd{data}\hldef{(}\hlsng{"sample_ecg"}\hldef{)}
\hldef{ecprob} \hlkwb{<-} \hlkwd{ECProb}\hldef{(sample_ecg)}
\hldef{n_sim} \hlkwb{<-} \hlnum{5}
\hldef{n_collections} \hlkwb{<-} \hlnum{10}
\hldef{collection_size_universe} \hlkwb{<-} \hlkwd{seq}\hldef{(}\hlnum{10}\hldef{,} \hlnum{1000}\hldef{,} \hlkwc{length.out} \hldef{= n_collections)}
\hldef{set_size_universe} \hlkwb{<-} \hlnum{10}\hlopt{:}\hlnum{100}
\hlkwd{set.seed}\hldef{(}\hlnum{1}\hldef{)}

\hldef{get_collection} \hlkwb{<-} \hlkwa{function}\hldef{(}\hlkwc{target_n_sets}\hldef{)\{}
  \hldef{n_sets} \hlkwb{<-} \hlkwd{round}\hldef{(target_n_sets)}
  \hldef{sets} \hlkwb{<-} \hlkwd{lapply}\hldef{(}\hlnum{1}\hlopt{:}\hldef{n_sets,} \hlkwa{function}\hldef{(}\hlkwc{x}\hldef{)\{}
    \hldef{set_size} \hlkwb{<-} \hlkwd{sample}\hldef{(set_size_universe,} \hlnum{1}\hldef{)}
    \hlkwd{sample}\hldef{(ecprob}\hlopt{@}\hlkwc{names}\hldef{, set_size)}
  \hldef{\})}
  \hldef{set_membership_dt} \hlkwb{<-} \hlkwd{data.table}\hldef{(}
    \hlkwc{set_id} \hldef{=} \hlkwd{rep}\hldef{(}\hlkwd{seq_along}\hldef{(sets),} \hlkwd{lengths}\hldef{(sets)),}
    \hlkwc{element} \hldef{=} \hlkwd{unlist}\hldef{(sets)}
  \hldef{)}
  \hlkwd{return}\hldef{(set_membership_dt)}
\hldef{\}}

\hldef{collections} \hlkwb{<-} \hlkwd{lapply}\hldef{(collection_size_universe, get_collection)}
\hldef{results_list} \hlkwb{<-} \hlkwd{lapply}\hldef{(collections,} \hlkwa{function}\hldef{(}\hlkwc{set_membership_dt}\hldef{)\{}
  \hldef{sets_dt} \hlkwb{<-} \hlkwd{data.table}\hldef{(}\hlkwc{set_id} \hldef{=} \hlkwd{unique}\hldef{(set_membership_dt}\hlopt{$}\hldef{set_id))}
  \hldef{times} \hlkwb{<-} \hlkwd{numeric}\hldef{(n_sim)}
  \hldef{res} \hlkwb{<-} \hlkwa{NULL}
  \hlkwa{for}\hldef{(k} \hlkwa{in} \hlnum{1}\hlopt{:}\hldef{n_sim)\{}
    \hldef{start_time} \hlkwb{<-} \hlkwd{Sys.time}\hldef{()}
    \hlkwd{capture.output}\hldef{(}
      \hldef{res} \hlkwb{<-} \hlkwd{calculate_in_stats_fast_vectorized}\hldef{(ecprob, sets_dt, set_membership_dt)}
    \hldef{)}
    \hldef{end_time} \hlkwb{<-} \hlkwd{Sys.time}\hldef{()}
    \hldef{times[k]} \hlkwb{<-} \hlkwd{as.numeric}\hldef{(end_time} \hlopt{-} \hldef{start_time,} \hlkwc{units} \hldef{=} \hlsng{"secs"}\hldef{)}
  \hldef{\}}
  \hldef{complexity} \hlkwb{<-} \hlkwd{sum}\hldef{(res}\hlopt{$}\hldef{set_size,} \hlkwc{na.rm} \hldef{=} \hlnum{TRUE}\hldef{)}
  \hlkwd{data.table}\hldef{(}\hlkwc{complexity} \hldef{= complexity,} \hlkwc{time} \hldef{=} \hlkwd{min}\hldef{(times))}
\hldef{\})}

\hldef{results_dt} \hlkwb{<-} \hlkwd{rbindlist}\hldef{(results_list)}

\hldef{lm_model} \hlkwb{<-} \hlkwd{lm}\hldef{(time} \hlopt{~} \hldef{complexity,} \hlkwc{data} \hldef{= results_dt)}
\hldef{r_sq} \hlkwb{<-} \hlkwd{summary}\hldef{(lm_model)}\hlopt{$}\hldef{r.squared}

\hlkwd{plot}\hldef{(results_dt}\hlopt{$}\hldef{complexity, results_dt}\hlopt{$}\hldef{time,} \hlkwc{pch}\hldef{=}\hlnum{19}\hldef{,} \hlkwc{cex}\hldef{=}\hlnum{1.2}\hldef{,}
     \hlkwc{xlab} \hldef{=} \hlsng{"Complexity (total vertices processed)"}\hldef{,}
     \hlkwc{ylab} \hldef{=} \hlsng{"Execution time (s)"}\hldef{,}
     \hlkwc{main} \hldef{=} \hlkwd{paste}\hldef{(}\hlsng{"Linear complexity verification\textbackslash{}nR-squared ="}\hldef{,} \hlkwd{round}\hldef{(r_sq,} \hlnum{4}\hldef{)))}
\hlkwd{abline}\hldef{(lm_model,} \hlkwc{col} \hldef{=} \hlsng{"darkgrey"}\hldef{,} \hlkwc{lwd}\hldef{=}\hlnum{2}\hldef{)}
\hlkwd{points}\hldef{(results_dt}\hlopt{$}\hldef{complexity, results_dt}\hlopt{$}\hldef{time,} \hlkwc{pch}\hldef{=}\hlnum{19}\hldef{,} \hlkwc{cex}\hldef{=}\hlnum{1.2}\hldef{)}
\end{alltt}
\end{kframe}

{\centering \includegraphics[width=0.7\textwidth]{figure/complexity_in_simulation-1} 

}


\end{knitrout}


The execution time scales linearly with the optimal time complexity, which is the sum of the set sizes in each set collection used as input.

The fast approximation of $p_{ij}$ also enables optimal computation of "between" connectedness statistics.

\subsection{Between sets connectedness}

Consider two disjoint vertex sets $S_1$ and $S_2$ with sizes $n_1$ and $n_2$. With the fast approximation, the $\lambda$ parameter is
\begin{equation}
\lambda_{btw} = \sum_{i \in S_1, j \in S_2} \frac{k_i k_j}{2M}.
\end{equation}
A naive implementation summing over all pairs would have time complexity $\mathcal{O} \left( n_1 n_2 \right)$. However, the expression can be factored as the product of two independent sums:
\begin{equation}
\lambda_{btw} = \frac{1}{2M} \left( \sum_{i \in S_1} k_i \right) \left( \sum_{j \in S_2} k_j \right).
\end{equation}

This factorization reduces the calculation to simply summing the degrees within each set and multiplying the results. Consequently, the computation of $\lambda_{btw}$ has linear time complexity: $\mathcal{O}\left( n_1 + n_2 \right)$.

Fast calculation of 'between' edge-count statistics for a single pair of sets is implemented in method \texttt{edge\_count\_statistics()} of class \texttt{ECProb} by setting both arguments \texttt{set1} and \texttt{set2} to non-null vectors and by using \texttt{lambda\_method = "fast"}.

For multiple pairs of sets, the appropriatedmethod is
\texttt{calculate\_between\_stats\_fast\_vectorized()}. It implements the fast approximation and is fully vectorized in parts by using the \texttt{data.table} package \cite{datatable2025}. The following simulation confirms that it achieves optimal, linear here, time complexity.

\begin{knitrout}
\definecolor{shadecolor}{rgb}{0.969, 0.969, 0.969}\color{fgcolor}\begin{kframe}
\begin{alltt}
\hlkwd{library}\hldef{(data.table)}
\hlkwd{library}\hldef{(EdgeCount)}
\hlkwd{data}\hldef{(}\hlsng{"sample_ecg"}\hldef{)}
\hldef{ecprob} \hlkwb{<-} \hlkwd{ECProb}\hldef{(sample_ecg)}

\hldef{n_sim} \hlkwb{<-} \hlnum{10}
\hldef{n_collections} \hlkwb{<-} \hlnum{10}
\hldef{n_pairs_universe} \hlkwb{<-} \hlkwd{seq}\hldef{(}\hlnum{500}\hldef{,} \hlnum{5000}\hldef{,} \hlkwc{length.out} \hldef{= n_collections)}
\hldef{set_size_universe} \hlkwb{<-} \hlnum{10}\hlopt{:}\hlnum{50}
\hlkwd{set.seed}\hldef{(}\hlnum{123}\hldef{)}

\hldef{get_between_collection} \hlkwb{<-} \hlkwa{function}\hldef{(}\hlkwc{n_pairs}\hldef{)\{}

  \hldef{n_pairs} \hlkwb{<-} \hlkwd{round}\hldef{(n_pairs)}
  \hldef{total_sets} \hlkwb{<-} \hlnum{2} \hlopt{*} \hldef{n_pairs}

  \hldef{sets} \hlkwb{<-} \hlkwd{lapply}\hldef{(}\hlnum{1}\hlopt{:}\hldef{total_sets,} \hlkwa{function}\hldef{(}\hlkwc{x}\hldef{) \{}
    \hlkwd{sample}\hldef{(ecprob}\hlopt{@}\hlkwc{names}\hldef{,} \hlkwd{sample}\hldef{(set_size_universe,} \hlnum{1}\hldef{))}
  \hldef{\})}

  \hldef{set_membership_dt} \hlkwb{<-} \hlkwd{data.table}\hldef{(}
    \hlkwc{set_id} \hldef{=} \hlkwd{rep}\hldef{(}\hlkwd{seq_along}\hldef{(sets),} \hlkwd{lengths}\hldef{(sets)),}
    \hlkwc{element} \hldef{=} \hlkwd{unlist}\hldef{(sets)}
  \hldef{)}

  \hldef{pairs_dt} \hlkwb{<-} \hlkwd{data.table}\hldef{(}
    \hlkwc{set1} \hldef{=} \hlkwd{seq}\hldef{(}\hlnum{1}\hldef{, total_sets,} \hlkwc{by} \hldef{=} \hlnum{2}\hldef{),}
    \hlkwc{set2} \hldef{=} \hlkwd{seq}\hldef{(}\hlnum{2}\hldef{, total_sets,} \hlkwc{by} \hldef{=} \hlnum{2}\hldef{)}
  \hldef{)}

  \hlkwd{return}\hldef{(}\hlkwd{list}\hldef{(}\hlkwc{pairs} \hldef{= pairs_dt,} \hlkwc{membership} \hldef{= set_membership_dt))}
\hldef{\}}

\hldef{collections_btw} \hlkwb{<-} \hlkwd{lapply}\hldef{(n_pairs_universe, get_between_collection)}

\hldef{results_list_btw} \hlkwb{<-} \hlkwd{lapply}\hldef{(collections_btw,} \hlkwa{function}\hldef{(}\hlkwc{input_data}\hldef{)\{}

  \hldef{pairs_dt} \hlkwb{<-} \hldef{input_data}\hlopt{$}\hldef{pairs}
  \hldef{membership_dt} \hlkwb{<-} \hldef{input_data}\hlopt{$}\hldef{membership}

  \hldef{times} \hlkwb{<-} \hlkwd{numeric}\hldef{(n_sim)}
  \hldef{res} \hlkwb{<-} \hlkwa{NULL}

  \hlkwa{for}\hldef{(k} \hlkwa{in} \hlnum{1}\hlopt{:}\hldef{n_sim)\{}
    \hldef{start_time} \hlkwb{<-} \hlkwd{Sys.time}\hldef{()}
    \hldef{temp_res} \hlkwb{<-} \hlkwa{NULL}
    \hlkwd{capture.output}\hldef{(}
      \hldef{temp_res} \hlkwb{<-} \hlkwd{calculate_between_stats_fast_vectorized}\hldef{(ecprob, pairs_dt, membership_dt)}
    \hldef{)}
    \hldef{res} \hlkwb{<-} \hldef{temp_res}
    \hldef{end_time} \hlkwb{<-} \hlkwd{Sys.time}\hldef{()}
    \hldef{times[k]} \hlkwb{<-} \hlkwd{as.numeric}\hldef{(end_time} \hlopt{-} \hldef{start_time,} \hlkwc{units} \hldef{=} \hlsng{"secs"}\hldef{)}
  \hldef{\}}

  \hldef{complexity} \hlkwb{<-} \hlkwd{sum}\hldef{(res}\hlopt{$}\hldef{size1,} \hlkwc{na.rm} \hldef{=} \hlnum{TRUE}\hldef{)} \hlopt{+} \hlkwd{sum}\hldef{(res}\hlopt{$}\hldef{size2,} \hlkwc{na.rm} \hldef{=} \hlnum{TRUE}\hldef{)}

  \hlkwd{data.table}\hldef{(}\hlkwc{complexity} \hldef{= complexity,} \hlkwc{time} \hldef{=} \hlkwd{min}\hldef{(times))}
\hldef{\})}

\hldef{results_dt_btw} \hlkwb{<-} \hlkwd{rbindlist}\hldef{(results_list_btw)}

\hldef{lm_model_btw} \hlkwb{<-} \hlkwd{lm}\hldef{(time} \hlopt{~} \hldef{complexity,} \hlkwc{data} \hldef{= results_dt_btw)}
\hldef{r_sq_btw} \hlkwb{<-} \hlkwd{summary}\hldef{(lm_model_btw)}\hlopt{$}\hldef{r.squared}

\hlkwd{plot}\hldef{(results_dt_btw}\hlopt{$}\hldef{complexity, results_dt_btw}\hlopt{$}\hldef{time,} \hlkwc{pch}\hldef{=}\hlnum{19}\hldef{,} \hlkwc{cex}\hldef{=}\hlnum{1.2}\hldef{,}
     \hlkwc{xlab} \hldef{=} \hlsng{"Complexity (sum of set sizes)"}\hldef{,}
     \hlkwc{ylab} \hldef{=} \hlsng{"Execution time (s)"}\hldef{,}
     \hlkwc{main} \hldef{=} \hlkwd{paste}\hldef{(}\hlsng{"Between-set linear complexity\textbackslash{}nR-squared ="}\hldef{,} \hlkwd{round}\hldef{(r_sq_btw,} \hlnum{4}\hldef{)))}
\hlkwd{abline}\hldef{(lm_model_btw,} \hlkwc{col} \hldef{=} \hlsng{"darkgrey"}\hldef{,} \hlkwc{lwd}\hldef{=}\hlnum{2}\hldef{)}
\hlkwd{points}\hldef{(results_dt_btw}\hlopt{$}\hldef{complexity, results_dt_btw}\hlopt{$}\hldef{time,} \hlkwc{pch}\hldef{=}\hlnum{19}\hldef{,} \hlkwc{cex}\hldef{=}\hlnum{1.2}\hldef{)}
\end{alltt}
\end{kframe}

{\centering \includegraphics[width=0.7\textwidth]{figure/complexity_between_simulation-1} 

}


\end{knitrout}


\bibliographystyle{plain}
\bibliography{theory}

\end{document}



\section{Algorithmic Optimization}

A key feature of \texttt{EdgeCount} is its speed. Calculating $\lambda$ involves summing probabilities over pairs of vertices. A naive implementation requires iterating over all pairs, resulting in quadratic time complexity. However, by utilizing the "Fast" approximation (Equation \ref{eq:fast_pij}), we can factorize these sums to achieve linear time complexity.

\subsection{Within-Set Connectedness}

Consider a set of vertices $S \subseteq V$ with size $n = |S|$. We wish to calculate the expected number of edges $\lambda_{in}$ existing internally among members of $S$.
\begin{equation}
\lambda_{in} = \sum_{\{i,j\} \subseteq S, i < j} p_{ij} \approx \sum_{i < j} \frac{k_i k_j}{2M}
\end{equation}
A naive summation requires $O(n^2)$ operations. To optimize this, we recall the algebraic identity for the square of a sum:
\begin{equation}
\left( \sum_{i \in S} k_i \right)^2 = \sum_{i \in S} k_i^2 + 2 \sum_{i < j} k_i k_j
\end{equation}
Rearranging for the cross-terms:
\begin{equation}
\sum_{i < j} k_i k_j = \frac{1}{2} \left[ \left( \sum_{i \in S} k_i \right)^2 - \sum_{i \in S} k_i^2 \right]
\end{equation}
Substituting this into the expression for $\lambda_{in}$:
\begin{equation}
\lambda_{in} \approx \frac{1}{2M} \cdot \frac{1}{2} \left[ \left( \sum_{i \in S} k_i \right)^2 - \sum_{i \in S} k_i^2 \right] = \frac{1}{4M} \left[ \left( \sum_{i \in S} k_i \right)^2 - \sum_{i \in S} k_i^2 \right]
\end{equation}
\textbf{Complexity:} We now only need to compute $\sum k_i$ and $\sum k_i^2$. This requires iterating through $S$ once, reducing the complexity to $O(n)$. This optimization is implemented in the package function \texttt{calculate\_in\_stats\_fast\_vectorized}.

\subsection{Between-Set Connectedness}

Consider two disjoint sets of vertices $S_1$ and $S_2$. We wish to calculate the expected number of edges $\lambda_{bt}$ bridging these two sets.
\begin{equation}
\lambda_{bt} = \sum_{i \in S_1} \sum_{j \in S_2} p_{ij} \approx \sum_{i \in S_1} \sum_{j \in S_2} \frac{k_i k_j}{2M}
\end{equation}
A naive summation requires $O(|S_1| \cdot |S_2|)$ operations. Because the terms $k_i$ and $k_j$ are independent in the product, we can factor the sum:
\begin{equation}
\lambda_{bt} \approx \frac{1}{2M} \left( \sum_{i \in S_1} k_i \right) \left( \sum_{j \in S_2} k_j \right)
\end{equation}
\textbf{Complexity:} We sum the degrees in $S_1$ ($O(|S_1|)$) and $S_2$ ($O(|S_2|)$) separately and multiply the results. The total complexity is $O(|S_1| + |S_2|)$, which is linear relative to the input size. This optimization is implemented in \texttt{calculate\_between\_stats\_fast\_vectorized}.

\section{Effect Size: The Anscombe Ratio}

While the p-value indicates significance, it is dependent on sample size. To measure the magnitude of connectedness, \texttt{EdgeCount} employs the log Anscombe Ratio ($lAr$), which stabilizes the variance of Poisson-distributed variables.

\begin{equation}
lAr = \frac{1}{2} \log_2 \left( \frac{x + 3/8}{\lambda + 3/8} \right)
\end{equation}
where $x$ is the observed edge count and $\lambda$ is the expected edge count.
\begin{itemize}
    \item $lAr > 0$: The set is more connected than expected (enrichment).
    \item $lAr < 0$: The set is less connected than expected (depletion).
\end{itemize}
The addition of $3/8$ is an optimal correction for bias in the log transformation of Poisson variables \cite{WAP}.

\section{Vertex Set Enrichment Analysis (VSEA)}

The VSEA method implemented in \texttt{run\_vsea} adapts the principles of Gene Set Enrichment Analysis \cite{subramanian2005} to graph topology.

Standard GSEA uses a Kolmogorov-Smirnov-like running sum statistic. In VSEA, the "local" correlation at rank $i$ is replaced by the edge-count probability framework derived by \cite{JCB}. As we traverse the ranked list of elements, at step $i$ (considering the top $i$ elements as set $S_i$), we calculate:
\begin{enumerate}
    \item The observed edges $x_i$ between $S_i$ and a target Term $T$.
    \item The expected edges $\lambda_i$ using the RGGED model.
    \item The running score based on the Log Anscombe Ratio.
\end{enumerate}

Crucially, because $\lambda_i$ accounts for the degrees of the elements in $S_i$, \textbf{VSEA inherently penalizes hubs}. If the top-ranked elements are merely "sticky" (high degree) but not specifically connected to Term $T$, $\lambda_i$ will be very high, reducing the $lAr$ score. This distinguishes VSEA from standard hypergeometric enrichment which only counts overlaps, ignoring the varying likelihood of connections for high-degree elements.


